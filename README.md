# MiniGPT
MiniGPT is a GPT-2-inspired decoder-only Transformer built with PyTorch. It implements tiktoken tokenization, learnable embeddings, causal multi-head attention, feed-forward networks, LayerNorm, residual connections, weight tying, and an end-to-end training pipeline for learning how GPT-style language models work.
